namespace Mtf.Controls.Sunell.SunellSdk
{
    public enum AVStreamFormat
    {
        RawStream = 1,
        TSMultiplexedStream = 2,
        RawEncryptedStream = 3,
        PSMultiplexedStream = 4
    }
}
